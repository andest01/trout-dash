'use strict';

/**
 * @ngdoc function
 * @name troutApp.controller:StreamCtrl
 * @description
 * # StreamCtrl
 * Controller of the troutApp
 */
angular.module('troutApp')
  .controller('StreamCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
